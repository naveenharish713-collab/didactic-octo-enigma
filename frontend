terraform {
  required_version = ">= 1.5.0"

  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 5.0"
    }
  }
}

provider "aws" {
  region = "ap-south-1"
}

resource "aws_instance" "web" {
  ami           = "ami-0f5ee92e2d63afc18" # Example Amazon Linux 2023 AMI
  instance_type = "t2.micro"

  tags = {
    Name = "terraform-demo"
  }
}

output "instance_id" {
  
